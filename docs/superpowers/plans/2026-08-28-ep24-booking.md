# Ep24 — Mini-App Tập 5: Chữa bài tập mini-booking Đặt xe — Plan

**Goal:** Hoàn thiện mảnh cuối lưới dịch vụ: mini-booking 🛵 (bài tập tốt nghiệp ep23 → tập "chữa bài"). Video "Mini-App từ A đến Z · Tập 5" (~4 phút, phong cách chi tiết) + thumbnail + SEO. Dạy thêm 2 điểm mới: single-select state, và màn hình NGOÀI tab bar (Stack route + Link push + nút back — lý do: 6 tab là chật).

**Byte-match qua các tập:** repo tiến hóa nên đã tag `miniapp-tap-4` (trạng thái đúng như tập 4 quay); tập này sẽ tag `miniapp-tap-5`. SEO/README ghi chú: checkout tag để xem đúng code từng tập. index.tsx/tab layout được phép sửa tiếp (diff chính là nội dung bài).

**Code (demo-miniapps):**
- `packages/mini-booking`: @vietsuper/mini-booking, BookingScreen:
  - rides: Xe máy 🛵 15.000đ/km, Ô tô 4 chỗ 🚗 25.000đ/km, Ô tô 7 chỗ 🚐 32.000đ/km.
  - `ride: string | null` (SINGLE-select: bấm xe khác là THAY, không phải thêm), `km: number` khởi đầu 5.
  - Stepper − / + đổi km, kẹp Math.max(1, Math.min(20, ...)).
  - Ước tính = pricePerKm × km khi đã chọn xe; bottom bar hiện "Chọn xe để xem giá" khi chưa chọn.
  - Style đồng bộ FoodScreen/CinemaScreen (nút chọn đỏ → xanh "Đang chọn ✓").
- Host: KHÔNG thêm tab. `app/booking.tsx` (3 dòng); root `app/_layout.tsx` thêm `<Stack.Screen name="booking" options={{ headerShown: true, title: 'Đặt xe' }} />` (có nút back tự động); index.tsx: union thêm '/booking', card Đặt xe nhận route.
- Verify: npm install, tsc, expo export (host).

**Video (~11 scene, ~52 câu):** title chữa-bài → concept: vì sao KHÔNG thêm tab thứ 6 (tab bar chật; sảnh phụ đi qua Stack; nhiều mini của super app thật cũng vào từ home chứ không có tab) → diagram: Home card → Stack push /booking → back → code package.json+index (nhắc nhanh — công thức cũ) → code BookingScreen data + single-select (SO SÁNH với multi-select cinema: string|null vs string[]) → code stepper km + ước tính → code host: booking.tsx + Stack.Screen (headerShown + title + back) → code index.tsx diff 2 dòng → phone: home card sáng + booking chạy (chụp thật, chọn xe + tăng km) → concept tổng kết 5 căn hộ + khi nào tab khi nào stack → outro: grid full sáng, chúc mừng tốt nghiệp thật sự.

**Screenshots:** home (6 card đều sáng), booking chưa chọn xe, booking đã chọn Ô tô 4 chỗ + 7 km (ước tính 175.000đ).

Quy trình như cũ: build verify → screenshots → script (validate + byte/substring check) → TTS → Root Episode24/Thumb24 (shot, ảnh booking) → render + stills → SEO (thêm ghi chú tag) → review fable → merge + tag + push.
