# Ep25 — Mini-App Tập 6: mini-topup Nạp thẻ — hoàn thiện lưới 6/6 — Plan

**Goal:** Mini-app cuối cùng: Nạp thẻ điện thoại 📱. Video "Mini-App từ A đến Z · Tập 6 (hoàn thiện)" (~4 phút) + thumbnail + SEO. Grid ViệtSuper đạt 6/6 sáng — khoảnh khắc finale của series.

**Điểm dạy mới:** (1) MỘT màn hình – HAI trạng thái giao diện: form nạp ↔ màn "Nạp thành công" (conditional early-return theo state `done`, nút Nạp thêm reset cả 3 state); (2) hai lựa chọn phối hợp: nhà mạng (chips) + mệnh giá (lưới numColumns 3 — gọi lại bài FlatList ep15), derived `canPay = network && amount`.

**Code (demo-miniapps):**
- `packages/mini-topup`: @vietsuper/mini-topup, TopupScreen:
  - networks: Viettel 🔴, VinaPhone 🔵, MobiFone 🟡 (chips hàng ngang, chọn-một).
  - amounts: [10000, 20000, 50000, 100000, 200000, 500000] — lưới 3 cột card, chọn-một (viền đỏ khi chọn).
  - state: network (string|null), amount (number|null), done (bool). canPay = cả hai đã chọn.
  - Bottom bar: chưa đủ → "Chọn nhà mạng và mệnh giá"; đủ → nút "Nạp ngay · {amount}đ" → setDone(true).
  - done → early return màn success: ✅ to, "Nạp thành công!", dòng "{amount}đ cho thuê bao {network}", nút "Nạp thêm" reset network/amount/done.
  - Style đồng bộ series.
- Host: stack route như booking — `app/topup.tsx` 3 dòng; root `_layout.tsx` thêm Stack.Screen topup (headerShown, title 'Nạp thẻ', headerBackTitle 'Trang chủ'); index.tsx: union thêm '/topup', card topup nhận route. (Các file này đã chiếu ở tập trước → tag miniapp-tap-5 đã chốt trạng thái cũ; đây chính là công dụng của tag.)
- Verify: npm install, tsc, expo export.

**Video (~11 scene, ~52 câu):** title tập hoàn thiện → concept giải phẫu màn nạp thẻ (2 lựa chọn + 1 hành động + 1 màn cảm ơn; khái niệm UI-2-trạng-thái) → diagram: form —Nạp ngay→ success —Nạp thêm→ form → code data networks+amounts → code 3 state + canPay → code success early-return + reset (điểm nhấn) → code lưới mệnh giá numColumns (ôn ep15) → code host nhanh (topup.tsx + Stack.Screen + card — công thức đã thuộc, 1 scene) → phone: form đã chọn Viettel+100k, success screen, home 6/6 SÁNG (finale) → concept tổng kết 6 căn hộ + toàn cảnh kiến trúc → outro trọn vẹn series + tag miniapp-tap-6.

**Screenshots:** topup-selected (Viettel + 100.000đ + nút Nạp ngay), topup-success, home-6-6.png.

Quy trình chuẩn như ep24. Sau merge: `git tag miniapp-tap-6` + push. Ràng buộc cũ giữ nguyên.
