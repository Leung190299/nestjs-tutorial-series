import 'package:flutter/material.dart';

class F2Page extends StatelessWidget {
  const F2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('F2 · itemExtent + const item')),
      body: const Center(child: Text('Chưa làm')),
    );
  }
}
