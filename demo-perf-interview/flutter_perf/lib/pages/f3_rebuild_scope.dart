import 'package:flutter/material.dart';

class F3Page extends StatelessWidget {
  const F3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('F3 · setState cả trang vs tách widget')),
      body: const Center(child: Text('Chưa làm')),
    );
  }
}
