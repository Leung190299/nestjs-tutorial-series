import 'package:flutter/material.dart';

class F4Page extends StatelessWidget {
  const F4Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('F4 · Infinite scroll + guard')),
      body: const Center(child: Text('Chưa làm')),
    );
  }
}
