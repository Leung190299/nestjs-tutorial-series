import 'package:flutter/material.dart';

class F5Page extends StatelessWidget {
  const F5Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('F5 · Ảnh — cacheWidth + cache')),
      body: const Center(child: Text('Chưa làm')),
    );
  }
}
